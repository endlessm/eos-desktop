# It's the only way we have to let gnome-shell find the private library it
# links against through libgnome-shell.so
eos-shell: binary-or-shlib-defines-rpath usr/bin/eos-shell /usr/lib/gnome-bluetooth
eos-shell: binary-or-shlib-defines-rpath usr/lib/eos-shell/libgnome-shell.so /usr/lib/gnome-bluetooth
